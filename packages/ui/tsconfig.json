{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // ui is the only package that needs DOM types and JSX
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx"
  },
  "include": ["src"]
}
